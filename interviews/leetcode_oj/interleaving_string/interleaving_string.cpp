#include <iostream>

using namespace std;

int main(int argc, const char* argv[]){
    string one(argv[1]);
    string two(argv[2]);
    string three(argv[3]);
    int i, j, k;
    k = 0, i = 0, j = 0;
    while(k <= three.size()){
         
        k++;
    }
    return 0; 
}
